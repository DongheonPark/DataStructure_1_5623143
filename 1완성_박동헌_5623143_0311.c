#include <stdio.h>

int main() {
    int num;

    printf("?????? ????????: ");
    scanf("%d", &num);

    printf("???? ?????? 32??? ???: ");
    for (int i = 31; i >= 0; i--) {
        int change = num >> i & 1;
        printf("%d", change);
    }
}